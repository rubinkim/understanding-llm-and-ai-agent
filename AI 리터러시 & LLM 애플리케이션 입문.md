# AI 리터러시 & LLM 애플리케이션 입문

강사님: 임우재님
날짜: 2026년 7월 24일

오늘 배운 산출물(비교표, 리팩토링 결과, 자동화 시나리오)은 GitHub 개인 Repository에 commit합니다.

### 생성형 AI의 이해 - LLM은 어떻게 작동하는가(Great AI Era, Why “Engineer”?)

기업에서는 “AI를 쓸까 말까”를 고민하는게 아니라 “누가, 어떤 권한으로, 어디까지 쓰게 할 것인가”입니다.

AI Agent Engineer는 “LLM이라는 부품을 잘 이해하고, 업무 흐름에 결합해 시스템으로 만드는 사람”입니다.

LLM이 전기라면 나는 그걸 Senior HealthCare Service라는 공장에 배선하는 사람입니다.

오늘은 첫번째로 LLM이라는 부품의 spec sheet를 읽는 훈련을 합니다. LLM의 동작 원리, 제조사별 라인업, LLM의 한계와 환각, 그리고 결합 방법(프롬프트)까지.

### LLM의 정체: 다음 token 맞히기 기계

LLM은 “지금까지의 글 다음에 올 token을 확률적으로 예측하는 기계”입니다.

“다음 토큰 예측”만으로 어떻게 대화가 되나요? 핵심은 반복입니다.

입력 —> 모델이 확률 계산 —> 가장 그럴듯한 토큰 하나를 뽑아 출력에 붙임 —> 끝신호가 나올 때까지 무한 반복…  우리가 보는 긴 답변은 이 예측을 수백~수천번 반복한 결과입니다.

이제 AI Agent Engineer 관점에서 살펴봅시다.

LLM은 “이해하는 기계”가 아니라 “그럴듯한 다음 토큰을 뽑는 기계”입니다. 그래서 잘하는 것(패턴이 많은 작업)과 못하는 것(정확한 계산, 최신 사실)이 명확히 갈립니다. 이 한계를 도구 연결(검색, 코드 실행, API)로 보완하는 것이 바로 Agent이고 이 과정 전체의 주제입니다. 

### LLM은 어떻게 만들어지는가: 학습 3단계

1단계(사전학습: Pre-training) —> 2단계(지도 미세조정: SFT) —> 3단계(인간 피드백 강화학습: RLHF)

모델마다 성격이 다른 이유는 3단계에서 각 회사가 다른 기준으로 튜닝했기 때문입니다. 

1단계 학습 데이터가 특정 시점까지만 있어서 그 이후의 사실은 모르기 때문에 지식 컷오프가 존재합니다.

3단계에서 “사람들이 좋아하는 답변”을 강화한다면 정확함보다는 그럴듯함이 강화되므로 가끔 이상하게 공손하거나 장황해지곤 합니다. 

### 엔지니어가 반드시 알아야 할 LLM의 한계 4가지

1. 환각(Hullucination): 모델은 다음 토큰 예측 기계이기 때문에 “모른다”보다 “그럴듯한 거짓”을 뽑는 경향이 있습니다. 금융 도메인에서 환각은 곧 “컴플라이언스 사고”입니다. 미니 프로젝트(금융 규제 상담 Agent)에서 “근거 문서를 제시하게 만드는 설계(RAG)”를 배우는 이유입니다. 
2. 지식 컷오프(Knowledge Cutoff): 학습 데이터가 끝나는 시점 이후의 일은 모릅니다. “오늘 KOSPI 지수”를 물으면 검색 도구가 없는 한 답할 수 없거나 옛날 데이터로 답합니다. 해결책은 도구 연결(웹 검색, API호출)입니다. 
3. 컨텍스트 윈도우(Context Window): 모델이 한 번의 대화에서 기억할 수 있는 토큰의 총량입니다. 요즘 모델은 200K~1M 토큰까지 커졌지만 무한이 아닙니다. 긴 대화의 앞부분을 “까먹는” 현상의 원인이고 대화가 길어질수록 응답 품질이 떨어지면 새 대화로 reset하는 게 정답인 경우가 많습니다.
4. 비결정성(Non-deterministic): 같은 질문에도 매번 조금씩 다른 답이 나옵니다. 확률적으로 토큰을 뽑기 때문입니다. 서비스를 만들 때 “항상 같은 형식의 출력”이 필요하면 프롬프트 형식을 강하게 고정하거나 API의 temperature 설정을 낮춥니다. —> “형식 고정” 실습을 할겁니다.

### 이미지/ 영상 생성 AI와 Muti-Modal: text만 생성하는 게 아니다.

생성형 AI의 축은 크게 3개입니다: 텍스트(LLM), 이미지, 영상

AI Agent Engineer는 텍스트와 더불어 나머지 둘도 “부품”으로 쓸 줄 알아야 합니다. 예를 들어 보고서 자동 생성 파이프라인에 삽화 자동 생성을 붙입니다.

이미지 생성 AI는 어떻게 동작하나요? 핵심 키워드는 디퓨전(Diffusion, 확산)입니다. 

1. 학습: 멀쩡한 이미지에 노이즈를 조금씩 추가해서 완전한 노이즈로 만드는 과정을 학습합니다.
2. 생성: 거꾸로 완전한 노이즈에서 시작해 “프롬프트 조건에 맞게” 노이즈를 조금씩 제거합니다.
3. 결과: 노이즈가 걷히면서 프롬프트에 맞는 이미지가 나타납니다. 

#### 도구 지도 한 장

이미지   GPT 이미지(ChatGPT 내장)       대화하면서 수정 가능, 텍스트 렌더링 강함    기획서 삽화, 목업

이미지   Gemini 이미지(nano banana)   인물 일관성 유지, 사진 편집에 강함                기존 이미지 편집, 합성

이미지   Midjourney                                  예술적 quality 최상급                                       브랜드, 고품질 비쥬얼

이미지   Stable Diffusion                          오픈소스, 로컬 실행 가능                                  대량 생산, 커스텀 학습

영상       Sora(Open AI)                              텍스트로 영상 생성                                           숏폼, 컨셉 영상

영상       Veo(Google)                                 고품질 + 오디오 동시 생성                              광고 시안, 프로토타입

영상       King/ Runway                               이미지를 움직이는 영상으로                           제품 이미지 활용

멀티모달:: 이미지를 읽는 능력이 실무에선 더 중요하다. 생성보다 실무 활용도가 더 높은 건 입력 멀티모달입니다. 요즘 LLM은 이미지를 입력으로 받아서 읽습니다.

Chart ScreenShot —> 데이터 해석,  손글씨 메모 —> 텍스트 변환 + 정리

에러 화면 캡쳐 —> 원인 분석,   문서 사진 —> 표로 구조화             

### 차트 이미지 분석

1. 아무 증권사 앱이나 네이버 금융에서 관심 종목의 1년 차트를 캡처
2. ChatGPT 또는 Claude에 이미지 첨부 후 아래 프롬프트 입력
첨부한 차트 이미지를 보고 아래를 정리해줘.
    1. 전체 추세를 한 문장으로
    2. 눈에 띄는 변곡점 2개와 대략적인 시기
    3. 이 차트만 보고 판단할 수 없는 것 3가지

굳이 c를 넣은 이유는 생성 AI 사용 시 주의 3가지 (금융권 프로젝트 기준)

1. 저작권: 생성 이미지의 상업적 사용 조건은 도구마다 다릅니다. 사내 규정 확인이 먼저입니다.
2. 딥페이크: 실존 인물 합성은 법적 문제가 됩니다. 데모라도 하지 않습니다.
3. 데이터 유출: 사내 문서, 고객 정보를 외부 AI에 올리는 것은 별도 승인 사항입니다. 실습에서는 항상 가상 데이터를 사용합니다. 

### 프롬프트 에니지어링 기초 - 4요소 프레임: 같은 모델, 완전 다른 결과

프롬프트 A: 보고서 써줘. 주제는 AI.

프롬프트 B: 너는 금융회사 디지털전략팀의 5년차 기획자야. 

상황: 경영진에게 “생성형 AI 사내 도입 검토” 1페이지 보고서를 제출해야 해. 경영진은 AI 지식이 거의   없고 비용과 리스크에 민감해.

작업: 생성형 AI 사내 도입 검토 보고서 초안을 작성해줘.

형식: 전체 1페이지 분량. 구성은 추진 배경(3줄)/ 기대 효과(표)/ 예상 리스크와 대응(표)/ 다음 단계 제안(3개). 문어체, 개조식으로.

모델은 그대로인데 출력 품질은 완전히 다릅니다. 차이는 딱 하나! 입력의 구조입니다. 이게 프롬프트 엔지니어링입니다. “작업 지시서를 잘 쓰는 기술”입니다. 

### 좋은 프롬프트의 4요소: 역할 - 맥락 - 작업 - 형식

        요소                       질문                                          예시                                           빠졌을 때 증상

1. 역할(Role)   누구로서 답해야 하나?  “너는 여신 심사 10년차 심사역이야”   일반인 수준의 밋밋한 답변
2. 맥락(Context) 어떤 상황?어떤 배경? “신입 교육용 자료야, 대상은 비전공자  대상, 수준이 안맞는 답변
3. 작업(Task)   정확히 무엇을 해야 하나?  “아래 약관에서 고객 불리 조항 3개 찾아줘” 동문서답, 범위이탈
4. 형식(Format)결과물은 어떤 모양? “표로 각항목 2줄이내 근거 조항 번호 표시 매번다른 형태, 후처리불가

4개를 전부 항상 쓸 필요는 없습니다. 간단한 질문엔 작업만으로도 충분합니다. 하지만 결과물을 시스템에 넣어야 하는 순간(자동화)부터는 형식이 생명입니다. 형식이 고정되지 않은 출력은 parsing할 수가 없기 때문입니다. 이게 사무직의 프롬프트와 엔지니어의 프롬프트가 갈라지는 지점입니다. 

### Before/ After로 감 잡기

사례 1: 요약 (Before —> After)

Before

이 기사 요약해줘.

After

너는 자산운용사의 리서치 어시스턴트야.
아래 기사를 펀드매니저의 아침 브리핑용으로 요약해줘.

형식

- 핵심 3줄 (각 줄 25자 이내)
- 시장 영향: 긍정 / 부정 / 중립 중 하나 + 근거 1줄
- 관련 섹터 태그 (예: #은행 #반도체)

[기사 붙여넣기]

After는 “누가 왜 읽는지”가 있어서 요약의 기준 자체가 달라집니다.

사례 2: 분류(Before —> After)

Before

이 고객 문의 어느 부서로 보내야 해?

After

너는 금융 고객센터의 문의 분류 시스템이야.
아래 고객 문의를 분류해줘.

분류 기준

- 카테고리: 계좌 / 대출 / 카드 / 앱오류 / 기타 중 하나만
- 긴급도: 상 / 중 / 하 (금전 피해 진행 중이면 무조건 상)
- 출력 형식 (JSON)

{"category": "", "urgency": "", "reason": "한 줄 근거"}

문의 내용

[문의 붙여넣기]

출력이 JSON으로 고정되면  이 프롬프트는 그대로 n8n workflow의 부품이 됩니다. 

사례 3: 금지 조건 걸기(Before —> After)

Before

이 상품 설명서 쉽게 설명해줘.

After

너는 은행 창구의 상담 직원이야. 아래 금융상품 설명서를 고객에게 설명해줘.

지침

- 중학생도 이해할 수 있는 말로
- 반드시 원문에 있는 내용만 사용해. 없는 내용은 "설명서에 없는 내용입니다"라고 말해
- 수익률 언급 시 반드시 "원금 손실 가능성"을 같이 언급해
- 확정적 투자 권유 표현(무조건, 확실히 등) 금지

[설명서 붙여넣기]

“하지 마라” 조건은 금융 도메인 프롬프트의 핵심입니다. 미니 프로젝트의 규제 상담 Agent에서 이 패턴이 그대로 확장됩니다. 

### 구조화 팁 3가지

1. 구분자를 써라: 지시와 데이터를 # 제목, —-, [대괄호] 등으로 명확히 분리. 모델이 “어디까지가 명령이고 어디부터가 재료인지” 헷갈리지 않게.
2. 단계를 나눠라: “1단계: 사실 추출 —> 2단계: 분류 —> 3단계: 요약”처럼 순서를 명시하면 복잡한 작업의 품질이 올라갑니다.
3. 출력 예시를 보여줘라: 원하는 출력의 샘플을 1개 넣어주는 것만으로도 형식 준수율이 크게 올라갑니다. (이게 Few-shot의 시작입니다.)

다음 세션 예고: 다음 강의일에는 오늘의 4대 요소(역할, 문맥, 작업, 형식)위에 Few-shot, Chain-of-Thought, ReAct같은 고급 패턴을 쌓습니다. 오늘 4요쇼가 몸에 배야 합니다.